
Sasha's Whimsical Birthday

```
    body {
        margin: 0;
        padding: 0;
        display: flex;
        justify-content: center;
        align-items: center;
        min-height: 100vh;
        background: var(--background-gradient);
        font-family: 'Montserrat', sans-serif;
        overflow: hidden; /* Prevent scrollbars during animations */
    }

    /* Animated Shimmer Background */
    body::before {
        content: '';
        position: absolute;
        top: 0; left: 0; width: 100%; height: 100%;
        background: linear-gradient(-45deg, #ff9a9e, #fad0c4, #fad0c4, #ffd1ff);
        background-size: 400% 400%;
        animation: gradientShift 15s ease infinite;
        opacity: 0.6;
        z-index: -1;
    }

    @keyframes gradientShift {
        0% { background-position: 0% 50%; }
        50% { background-position: 100% 50%; }
        100% { background-position: 0% 50%; }
    }

    /* Container for Floating Elements */
    .background-decor {
        position: absolute;
        width: 100%;
        height: 100%;
        overflow: hidden;
        z-index: -1;
    }

    /* Floating Stars/Circles */
    .decor-item {
        position: absolute;
        background: rgba(255, 255, 255, 0.7);
        border-radius: 50%;
        opacity: 0;
        animation: float-up infinite linear;
    }

    @keyframes float-up {
        0% { transform: translateY(100vh) rotate(0deg); opacity: 0; }
        10% { opacity: 1; }
        90% { opacity: 1; }
        100% { transform: translateY(-10vh) rotate(360deg); opacity: 0; }
    }

    /* Main Card Style with Glassmorphism */
    .birthday-card {
        background: rgba(255, 255, 255, 0.6);
        backdrop-filter: blur(15px);
        border-radius: 30px;
        padding: 50px;
        width: 90%;
        max-width: 500px;
        box-shadow: 0 20px 50px rgba(0, 0, 0, 0.1);
        border: 1px solid rgba(255, 255, 255, 0.4);
        text-align: center;
        opacity: 0;
        transform: translateY(30px);
        animation: cardFadeIn 1s ease-out forwards;
        position: relative;
    }

    @keyframes cardFadeIn {
        to { opacity: 1; transform: translateY(0); }
    }

    /* Title Animation */
    .birthday-card h1 {
        font-family: 'Playfair Display', serif;
        color: var(--primary);
        font-size: 3rem;
        margin-bottom: 15px;
        animation: titleGlow 2s ease-in-out infinite alternate;
    }

    @keyframes titleGlow {
        from { text-shadow: 0 0 5px rgba(255, 133, 162, 0.5); }
        to { text-shadow: 0 0 15px rgba(255, 133, 162, 0.8), 0 0 20px rgba(255, 133, 162, 0.5); }
    }

    .birthday-card p {
        font-size: 1.1rem;
        color: var(--text);
        line-height: 1.6;
        margin-bottom: 30px;
    }

    /* Interactive Cake and Candles */
    .cake-container {
        position: relative;
        margin: 40px auto;
        width: 150px;
        height: 150px;
        cursor: pointer;
    }

    /* Stylized Cake Base */
    .cake {
        width: 100%;
        height: 80%;
        background: #f7d9c4;
        border-radius: 20px;
        position: absolute;
        bottom: 0;
        box-shadow: 0 10px 20px rgba(0,0,0,0.1);
    }

    .cake-top {
        width: 110%;
        height: 40px;
        background: #fff;
        border-radius: 50%;
        position: absolute;
        top: -20px;
        left: -5%;
        box-shadow: inset 0 -5px 10px rgba(0,0,0,0.05);
    }

    /* Stylized Candles */
    .candle {
        width: 10px;
        height: 50px;
        background: #ffd1ff;
        position: absolute;
        top: -40px;
        border-radius: 5px;
        box-shadow: 0 2px 5px rgba(0,0,0,0.1);
    }

    .candle-1 { left: 30px; animation: flicker 1.5s infinite alternate; }
    .candle-2 { left: 70px; animation: flicker 1.8s infinite alternate 0.2s; }
    .candle-3 { left: 110px; animation: flicker 2s infinite alternate 0.4s; }

    /* Flame animation */
    .candle::before {
        content: '';
        position: absolute;
        width: 12px;
        height: 18px;
        background: var(--accent);
        border-radius: 50% 50% 35% 35%;
        top: -15px;
        left: -1px;
        box-shadow: 0 0 10px var(--accent), 0 0 15px #fff;
        opacity: 1;
        transition: opacity 0.5s ease;
    }

    @keyframes flicker {
        0% { transform: scaleY(1); }
        100% { transform: scaleY(1.05); }
    }

    /* Extinguished state */
    .birthday-card.blown-out .candle::before {
        opacity: 0;
    }

    /* Personalized Message Area */
    .personal-message {
        background: rgba(255, 255, 255, 0.4);
        border-radius: 15px;
        padding: 20px;
        margin-top: 20px;
        border: 1px solid rgba(255, 255, 255, 0.3);
        font-style: italic;
    }

</style>

```

```
<!-- Floating Background Decorations -->
<div class="background-decor">
    <!-- JS will populate items here -->
</div>

<!-- Main Content Card -->
<div class="birthday-card" id="birthdayCard">
    <h1>Happy Birthday, Sasha!</h1>
    <p>Wishing you a day filled with laughter, love, and endless joy! Click the cake to make a wish and blow out the candles!</p>

    <!-- Interactive Cake -->
    <div class="cake-container" id="cake">
        <div class="cake"></div>
        <div class="cake-top"></div>
        <div class="candle candle-1"></div>
        <div class="candle candle-2"></div>
        <div class="candle candle-3"></div>
    </div>

    <div class="personal-message">
        “May your year ahead be as bright and beautiful as you are.”
    </div>
</div>

<script>
    document.addEventListener('DOMContentLoaded', () => {
        generateBackgroundDecor();
        setupInteractiveCake();
    });

    // Generate floating background particles
    function generateBackgroundDecor() {
        const container = document.querySelector('.background-decor');
        const itemCount = 50;

        for (let i = 0; i < itemCount; i++) {
            const item = document.createElement('div');
            item.classList.add('decor-item');
            
            // Randomize size, position, speed
            const size = Math.random() * 15 + 5 + 'px';
            item.style.width = size;
            item.style.height = size;
            item.style.left = Math.random() * 100 + '%';
            
            const duration = Math.random() * 10 + 5 + 's';
            item.style.animationDuration = duration;
            
            item.style.animationDelay = Math.random() * 5 + 's';

            container.appendChild(item);
        }
    }

    // Setup the interactive "candle blowing" action
    function setupInteractiveCake() {
        const cake = document.getElementById('cake');
        const card = document.getElementById('birthdayCard');

        cake.addEventListener('click', () => {
            if (!card.classList.contains('blown-out')) {
                card.classList.add('blown-out');
                createConfettiBurst(cake);
            }
        });
    }

    // Create a burst of confetti upon interaction
    function createConfettiBurst(element) {
        const colors = ['#ff85a2', '#a1c4fd', '#fff0b3', '#ffd1ff', '#fff'];
        const confettiCount = 30;

        for (let i = 0; i < confettiCount; i++) {
            const confetti = document.createElement('div');
            confetti.style.position = 'absolute';
            confetti.style.width = Math.random() * 8 + 4 + 'px';
            confetti.style.height = confetti.style.width;
            confetti.style.backgroundColor = colors[Math.floor(Math.random() * colors.length)];
            confetti.style.borderRadius = '50%';
            
            // Initial position (center of the cake)
            const rect = element.getBoundingClientRect();
            confetti.style.left = rect.left + rect.width / 2 + 'px';
            confetti.style.top = rect.top + rect.height / 2 + 'px';
            
            confetti.style.zIndex = '1000';
            document.body.appendChild(confetti);

            // Burst animation using JS (velocity)
            const angle = Math.random() * Math.PI * 2;
            const velocity = Math.random() * 8 + 4;
            const vx = Math.cos(angle) * velocity;
            const vy = Math.sin(angle) * velocity - 3; // Initial upward lift

            animateConfetti(confetti, vx, vy);
        }
    }

    // JS animation loop for confetti particles
    function animateConfetti(confetti, vx, vy) {
        let posX = parseFloat(confetti.style.left);
        let posY = parseFloat(confetti.style.top);
        let opacity = 1;
        const gravity = 0.2;

        const animationLoop = () => {
            vy += gravity; // Apply gravity
            posX += vx;
            posY += vy;
            opacity -= 0.01;

            confetti.style.left = posX + 'px';
            confetti.style.top = posY + 'px';
            confetti.style.opacity = opacity;

            if (opacity > 0) {
                requestAnimationFrame(animationLoop);
            } else {
                confetti.remove();
            }
        };

        requestAnimationFrame(animationLoop);
    }

</script>

```
